import 'package:flutter_test/flutter_test.dart';

// Basic smoke test – verifies the app can be imported and widgets rendered.
// (A full widget test requires the native library to be present; here we just
// verify the project is wired up correctly.)
void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
